# rustfmt configuration for the veles-crypto workspace.
# Match the existing style (4-space indent, trailing commas) and enforce
# a consistent line width that suits the JNI/FFI code with its longer types.

edition = "2024"
max_width = 100
use_field_init_shorthand = true
use_try_shorthand = true