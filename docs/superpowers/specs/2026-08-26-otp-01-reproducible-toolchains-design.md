# OTP-01: Add reproducible Chrome and cryptographic toolchains to Gradle — design

Status: Draft
Issue: raidenyn/veles-android#80
RFC: docs/rfcs/2026-08-21-otp-external-devices.md, section `### OTP-01`
Gate: Gate A (protocol and toolchain foundation)

## Purpose

Give the repository a deterministic way to install, lint, type-check, test,
build, and package the TypeScript MV3 extension, the Rust core placeholder
crate used to validate JNI and WASM plumbing (the real SPAKE2 core lands in
OTP-03), and the unsigned Tauri native-bridge release
inputs for Windows and macOS — all exposed as Gradle entry points, all pinned,
all byte-reproducible in clean environments.

OTP-01 delivers the toolchain foundation that Gate A and every later OTP task
depend on. It contains **no** Bluetooth transport, **no** cryptographic
protocol behavior, **no** UI, and **no** real OTP integration (RFC
"Exclusions" — preserved verbatim here as the acceptance boundary).

## Classified path

Architectural → decomposed into four sub-projects per user decision. Each
sub-project gets its own spec→plan→implement cycle. This document is the
single OTP-01 decomposition spec; sub-project 1a is designed in full here
because it is the first to be built. Sub-projects 1b/1c/1d are designed at
boundary level; their implementation specs are written when 1a is in review.

## Sub-projects and sequencing

| # | Sub-project | Deliverable | Feed into |
|---|---|---|---|
| 1a | TS/MV3 extension toolchain | `web-extension/` npm/Vite project + Gradle tasks + deterministic zip | OTP-02, 05, 07, 17, 18 |
| 1b | Rust core + JNI/WASM | `rust/` workspace, jniLibs for Android ABIs, `web-extension/rust-wasm/pkg` | OTP-02→03, 06 |
| 1c | Tauri native-host packaging | unsigned Windows/macOS bridge binaries, host manifests, per-user installer inputs | OTP-23, 24, 25 |
| 1d | Verification & supply chain | `verify/` extension + SBOM + license + byte-compare harness | Gate A, Gate D |

Canonical order: **1a → 1b → 1c → 1d**. Rationale: each sub-project must
leave artifacts in place for the next to consume; 1d verifies all three and
therefore cannot close until build outputs exist.

## Global decisions (apply to 1a / 1b / 1c / 1d)

1. **Toolchain provisioning is PATH-based, minimum-version declared.**
   node/npm/rustup/cargo are not downloaded by Gradle. `package.json
   engines.node`, `rust-toolchain.toml` channel, and Cargo minimum-version
   are the declared pins. `verify/` Docker images (1d) bake exact versions
   for byte-compare environments; developer machines need only satisfy the
   declared minimum.
2. **Lockfiles are the reproducibility contract.** `package-lock.json` and
   `Cargo.lock` are committed and builds run `--locked` / `npm ci`. Drift
   fails the build; never auto-fixed in CI.
3. **Artifacts under `build/`, with two named exceptions.** All Gradle task
   outputs land under the root `build/` and are wiped by `clean`. Two
   deliberate exceptions exist where a consumer outside Gradle expects a
   path inside a source tree:
   - `web-extension/dist/` — Vite's conventional output, produced by
     `extensionBuild`. Root `clean` is extended to also delete this
     directory.
   - `web-extension/rust-wasm/pkg/` — Vite expects WASM imports inside the
     project tree. Gitignored; regenerated by `rustWasm` (1b) before
     `extensionBuild` runs.
   No other generated artifacts live outside `build/`.
4. **New Gradle tasks join root `check` and ride into CI via a dedicated job.**
   Format/lint/typecheck/test tasks from every sub-project become
   dependencies of root `check` so any developer running `./gradlew check`
   gets the whole quality gate. CI does not run root `check` today (the
   existing JVM jobs invoke narrower entry points like `spotlessCheck` and
   `testDebugUnitTest`), so each toolchain sub-project also gets an
   explicit job in `.github/workflows/ci.yml` invoking its specific tasks —
   1a adds a `web-extension` job; 1b and 1c add theirs at implementation
   time.
5. **APK build is unaffected.** No new Gradle subprojects under
   `settings.gradle.kts` (`:app` remains the only included project). All
   new tasks live in the root `build.gradle.kts`, grouped under
   `extension`, `rust`, `native-bridge`, `verification` task groups. Clean
   clones without node/rust can still run `./gradlew :app:assembleDebug`.

## Sub-project 1a: TypeScript/MV3 extension toolchain (full design)

### Layout

```
web-extension/
  package.json            # name "@veles/web-extension", engines.node = ">=22.0.0"
  package-lock.json       # committed
  tsconfig.json           # strict, resolveJsonModule true (for version import)
  vite.config.ts          # two entries: background (service worker), content
  eslint.config.js        # ESLint 9 flat config with typescript-eslint
  .prettierrc             # prettier settings
  .prettierignore         # excludes node_modules/, dist/, package-lock.json
  .gitignore              # node_modules/, dist/, coverage/
  src/
    manifest.ts           # generates MV3 manifest.json for dist/
    background.ts         # service worker entry stub
    content.ts            # content-src entry stub
  test/
    smoke.test.ts         # vitest: manifest shape, entry exports (chrome stubbed)
    bundle.test.ts        # vitest: dist/ contents against canonical manifest
  public/                 # icons and statics copied by vite
```

### Gradle tasks (group `extension`, defined in root `build.gradle.kts`)

| Task | Command | Depends on | Output |
|---|---|---|---|
| `extensionToolchainCheck` | runs `node --version`, `npm --version`; fails if absent or below floors | — | — |
| `extensionInstall` | `npm ci` in `web-extension/` — skipped with a descriptive message if `web-extension/package-lock.json` is absent, so Android-only workflows still run | `extensionToolchainCheck` | `web-extension/node_modules` |
| `extensionFormat` | `npm run format:check` in `web-extension/` | `extensionInstall` | — |
| `extensionLint` | `npm run lint` in `web-extension/` | `extensionInstall` | — |
| `extensionTypecheck` | `npm run typecheck` in `web-extension/` | `extensionInstall` | — |
| `extensionTest` | `npm run test` in `web-extension/` — runs **only** `test/smoke.test.ts` (source-only; never reads `dist/`) | `extensionInstall` | — |
| `extensionBuild` | `npm run build` in `web-extension/` | `extensionInstall`, `extensionTypecheck` | `web-extension/dist/` |
| `validateExtensionManifest` | Gradle-side strict validation of `dist/manifest.json` | `extensionBuild` | — |
| `extensionArtifactTest` | `vitest run test/bundle.test.ts` against built `dist/` | `extensionBuild` | — |
| `extensionPackage` | deterministic zip of `dist/` plus sha256 sidecar | `extensionBuild`, `validateExtensionManifest`, `extensionArtifactTest` | `build/web-extension/veles-extension-<version>.zip` and `.zip.sha256` |

Wiring: root `check` depends on `extensionFormat`, `extensionLint`,
`extensionTypecheck`, `extensionTest`. `extensionBuild` and
`extensionPackage` are invoked explicitly (release-style), not from `check`.

### Deterministic packaging recipe (used by `extensionPackage`)

- ZIP entries sorted lexicographically by name.
- All entry mtimes set to the DOS epoch (Gradle `preserveFileTimestamps =
  false`) to avoid host clock leakage.
- No comment, no extra fields, no directory entries beyond those strictly
  required (`includeEmptyDirs = false`).
- Metadata charset fixed to UTF-8 (`metadataCharset = "UTF-8"`).
- Compression method fixed to DEFLATE (`entryCompression =
  ZipEntryCompression.DEFLATED`); the deflate *level* is left at the
  implementation default and is not claimed to be reproducibility-bearing
  under the committed-lockfile model. If a future verifier shows cross-host
  deflate-level drift, the recipe moves to `STORED` and documents the
  change.
- Permissions fixed to `0755` for directories, `0644` for files
  (`dirPermissions { unix("0755") }` / `filePermissions { unix("0644") }`).
- After the zip is produced, the task writes its hex SHA-256 into a sidecar
  file `veles-extension-<version>.zip.sha256` next to the zip in
  `build/web-extension/`.
- Before zipping, `validateExtensionManifest` asserts `dist/manifest.json`
  matches the pinned baseline exactly:
  - `manifest_version == 3`
  - `permissions` is exactly `[]` (not absent, not a non-list)
  - `host_permissions` is absent
  - `content_security_policy` is exactly `{ extension_pages: "script-src 'self'; object-src 'self'" }`
    — string equality on the whole CSP map, not substring checks.

### Error handling

| Failure | Behavior |
|---|---|
| `node`/`npm` missing from PATH | `extensionToolchainCheck` fails fast with `Could not find 'node' on PATH; install Node.js >= 22.0.0 (declared in web-extension/package.json engines.node)` — mirrors `verify/verify.sh` pin-error style. |
| `node`/`npm` present but below declared floor | `extensionToolchainCheck` fails with both actual and required versions printed. |
| `package-lock.json` drift (`npm ci` non-zero) | Hard fail. No `npm install` fallback. |
| Manifest/CSP guard trip | `validateExtensionManifest` fails naming the violated baseline. |
| `web-extension/package-lock.json` absent | All `extension*` tasks (including `extensionToolchainCheck`) skipped with descriptive messages — Android-only developer can run `./gradlew build` without node. |

### Versioning

The extension artifact name embeds `web-extension/package.json` `version`
(manually bumped per extension release). This is deliberately independent
of the Android app's git-tag-derived versionName — the extension and the
APK ship on separate tracks.

### Testing (sub-project 1a, before 1d wires byte-compare)

- `vitest run test/smoke.test.ts` covers source-only assertions: manifest
  generator emits expected MV3 shape, both entry points compile and export.
- `vitest run test/bundle.test.ts` — invoked by `extensionArtifactTest`
  **after** `extensionBuild` — asserts two things against built `dist/`:
  (1) the exact file set, as an inline expected list (currently
  `['background.js', 'content.js', 'manifest.json']`; any new chunk/asset
  fails the test and forces conscious review), and (2) that
  `dist/manifest.json` round-trips through `buildExtensionManifest()`.
- Byte-determinism is asserted in 1d via double-build hash-compare, not in
  1a. 1a's job is to make the zip recipe deterministic so 1d can compare
  hashes without surprises.

## Sub-project 1b: Rust core + JNI/WASM (boundary)

- **Layout:** `rust/` workspace with one crate `veles-crypto` exposing both
  `extern "C"` JNI shims and `wasm-bindgen` exports. Sibling crates for
  JNI/WASM bindings split only if feature gating becomes unmanageable.
- **Toolchain:** `rust/rust-toolchain.toml` pins the channel (e.g.
  `channel = "1.xx.x"`, `targets = [...]`, `components = ["clippy",
  "rustfmt"]`). Gradle does not fetch rustup; builds assume `cargo` on
  PATH satisfying the `rust-toolchain.toml`.
- **Placeholder content (per user):** `veles-crypto` exposes exactly one
  operation through both bindings, chosen at implementation time purely to
  exercise the plumbing — any deterministic function of input bytes to
  output bytes (a hash, a fixed transform, etc.) is acceptable. The choice
  is non-binding and carries no cryptographic meaning. No SPAKE2, no key
  material, no protocol-level state. Inputs/outputs use a simple
  byte-buffer contract that does not presuppose OTP-02's protocol
  decisions and will be replaced wholesale when OTP-03 lands.
- **Gradle tasks (group `rust`):**
  - `rustInstall` — no-op placeholder that validates `cargo` presence and
    `rust-toolchain.toml` parse, matching the `extensionInstall` style.
  - `rustJni` — `cargo build --locked --release --target
    aarch64-linux-android` (plus `armv7-linux-androideabi`,
    `x86_64-linux-android`), copies `.so` files into
    `app/src/main/jniLibs/<abi>/`. `assembleDebug`/`assembleRelease`
    depend on it, so a plain APK build pulls the .so automatically.
  - `rustWasm` — `wasm-pack build --target web --out-dir
    ../../web-extension/rust-wasm/pkg` (or `wasm-bindgen-cli` equivalent).
    Output committed-ignored; regenerated before `extensionBuild`.
  - `rustFormat` (`cargo fmt --all -- --check`), `rustLint` (`cargo
    clippy --all-targets --locked -- -D warnings`), `rustTest` (`cargo
    test --locked`). Wired into root `check`.
- **NDK:** uses the NDK installed by AGP under `local.properties`/SDK
  manager, no separate pin file inside `rust/`. NDK version recorded in
  `libs.versions.toml` (`ndk = "x.y.z"`) and propagated to
  `app/build.gradle.kts` `android.ndkVersion`.
- **Open question for 1b's own spec:** current text says `rustJni` copies
  `.so` files into `app/src/main/jniLibs/<abi>/`, which violates the global
  "artifacts under `build/`" decision from this document. 1b's spec must
  resolve this — the standard fix is to emit under
  `app/build/generated/jniLibs/<abi>/` and register that directory as an
  `android.sourceSets["main"].jniLibs.srcDir`. 1b's plan inherits that
  correction.

## Sub-project 1c: Tauri native-host packaging (boundary)

- **Layout:** `native-bridge/` at repo root, a Tauri 2.x headless app
  (`src-tauri/`, `tauri.conf.json`, minimal `index.html` as a placeholder
  entry that is never shown).
- **User-confirmed posture:** Tauri configured with **`windows = []`** and
  all default features disabled (per decision "Tauri headless lifecycle"
  assessed at Gate D). No tray, no menu, no autostart, no login item. The
  process exits on stdin EOF, matching Native Messaging semantics.
- **Toolchain:** `tauri.conf.json`'s own npm devDeps follow the same
  `engines.node` minimum + committed `package-lock.json` pattern as 1a.
- **Gradle tasks (group `native-bridge`):**
  - `bridgeInstall` (`npm ci` inside `native-bridge/`), `bridgeFormat`,
    `bridgeLint`, `bridgeTypecheck` if any TS config, `bridgeTest`
    (`cargo test --locked` inside `src-tauri/`).
  - `bridgeBuild` — `cargo tauri build --locked --no-bundle` producing
    the unsigned binary for the host OS.
  - `bridgePackage` — platform-specific:
    - **Windows:** zip payload containing the `.exe`, native-messaging
      host manifest JSON, and installer inputs (WiX / NSIS files Tauri
      emits) into `build/native-bridge/windows/`.
    - **macOS:** tar payload containing the `.app` bundle (unsigned),
      host manifest, installer inputs into `build/native-bridge/macos/`.
  - `bridgeManifests` — emits the Chrome native-messaging host manifest
    for each OS with exact host-origin fields pinned to the extension ID
    placeholder used in 1a.
- **Explicitly excluded:** no BLE calls, no crypto, no signed artifacts.
  Tauri signing env vars (`TAURI_SIGNING_PRIVATE_KEY` etc.) are documented
  as absent in CI. `tauri.conf.json` hard-codes unsigned output.

## Sub-project 1d: Verification & supply chain

- **Environment strategy is asymmetric, matching technical reality.** Pure
  Linux-side toolchains (Android via existing `verify/Dockerfile`, plus new
  `verify/Dockerfile.web` for the extension and `verify/Dockerfile.rust`
  for the Rust core) run inside Docker as full reference environments, giving
  byte-stable rebuilds of the extension zip and of the Rust `.so` / `.wasm`
  intermediates. Windows and macOS artifacts cannot be rebuilt inside Linux
  Docker (signing/notarization require Apple/MS SDKs that are not portable),
  so 1d treats **pinned** GitHub-hosted runner labels (`windows-2022`,
  `macos-15` — never `-latest`) as the *documented environments*. Each
  reference run pins (a) the versioned runner label from the workflow YAML,
  (b) the Rust toolchain via `rust-toolchain.toml`, (c) the Tauri CLI
  version from `package-lock.json`, and (d) on macOS the Xcode/SDK version
  explicit in the workflow YAML. Each run captures the observed runner
  image identity from the **`ImageOS` and `ImageVersion` environment
  variables the runner images export** (these are *not* properties of the
  GitHub Actions `runner` context — workflows read them as
  `$env:ImageOS` / `$env:ImageVersion` on Windows and `$ImageOS` /
  `$ImageVersion` on macOS/Linux) plus `RUNNER_ARCH`, asserts both are
  non-empty, and records them into the workflow log and into
  `SHA256SUMS.native-bridge` metadata. Byte-comparison across two CI runs
  proceeds only when all three recorded values match; on mismatch the
  comparison aborts with a "re-run on matched image" instruction. This is
  deliberately weaker than pinning a self-hosted image ID — that option is
  an extension point if self-hosted infrastructure becomes available
  later.
- **Compare script:** `verify/verify-all.sh` orchestrates: build in each
  Docker image on Linux and compare hashes; run the Windows and macOS CI
  jobs twice on the same **versioned runner label** (`windows-2022`,
  `macos-15`) and compare output hashes **only when the observed image
  identities match** (see the 1d environment-strategy bullet for how
  identity is captured). Mismatch aborts with a re-run instruction. Verify
  the aggregated results against `SHA256SUMS.toolchains`. Exit codes
  mirror `verify.sh` (0 match, 1 mismatch, 2 usage/pin error).
- **SBOM:** `cyclonedx-npm` for `web-extension/`, `cargo cyclonedx` for
  `rust/` and `native-bridge/src-tauri/`. Output JSON files into
  `build/sbom/{web-extension,rust,native-bridge}.cdx.json`.
- **License:** `license-checker-rseidelsohn` for npm, `cargo deny
  licenses` for Rust. Both operate on an explicit, reviewed policy written
  as SPDX allow/deny lists (not a hand-waved "no copyleft" rule). Default
  posture: strong copyleft (GPL-*, AGPL-*, LGPL-* without static-linking
  exception), source-availability (SSPL, BUSL, Elastic), and public-domain
  dedications (CC0, Unlicense) default-deny; permissive (MIT, Apache-2.0,
  BSD-*, ISC, Zlib) default-allow; anything unrecognised fails with the
  license text and a link to the allowlist file so the reviewer can decide
  consciously. Policy file lives at the repo root (`licenses.toml` for
  cargo-deny, `.license-policy.json` for license-checker) and is part of
  the 1d review surface.
- **Checksums:** `SHA256SUMS` files per artifact tree produced by the
  `*Package` tasks, uploaded as CI artifacts.

## Acceptance criteria (traceable to RFC OTP-01)

The RFC's acceptance paragraph has six clauses. OTP-01 closes when the four
clauses it owns (1, 2, 3, 6) are demonstrated. Clauses 4 and 5 are formally
**transferred to OTP-25's acceptance criteria** by the RFC amendment dated
2026-08-26 — they depend on real signing/notarization infrastructure that
is OTP-25 scope. This transfer avoids the dependency deadlock that would
otherwise arise: OTP-25 transitively depends on OTP-01 via
OTP-06/OTP-07/OTP-24,
so if OTP-01 could not close until OTP-25 finished, neither issue could
ever reach Ready in the RFC's dependency model. OTP-01's contribution to
clauses 4 and 5 is the stable unsigned-artifact ground truth and the
no-embedded-secrets guarantee. The amendment is recorded in the RFC
(after the OTP-01 entry's project fields) and on issue #80 so the
transfer is explicit, not silent.

| Clause | Owner | OTP-01 acceptance depends on it? |
|---|---|---|
| 1 (byte-identical artifacts in clean envs) | 1d (`verify-all.sh`) | **Yes** — OTP-01 closes when met |
| 2 (CI-safe Gradle entry points) | 1a+1b+1c, wired into CI by 1d | **Yes** |
| 3 (artifact content allow-list) | 1a (ext file-set) + 1b (APK ABIs) + 1c (bridge) | **Yes** |
| 4 (signing consumes CI credentials) | OTP-25 | **No** — transferred to OTP-25 |
| 5 (signed↔unsigned correspondence) | OTP-25 | **No** — transferred to OTP-25 |
| 6 (no runtime download / remote exec) | 1a+1b+1c, scanned by 1d | **Yes** |

### RFC clauses (verbatim), with sub-project mapping

1. *"Independent clean documented environments produce matching locked
   dependencies and byte-identical unsigned bridge binaries, bundle/package
   payloads, host manifests, and installer inputs."*
   → **1d.** For Linux-side artifacts (Android APK, extension ZIP, Rust
   `.so`/`.wasm`), `verify/verify-all.sh` builds in pinned Docker
   environments and compares hashes. For Windows/macOS — which cannot be
   reproduced inside Linux Docker — two CI runs on GitHub-hosted
   `windows-2022` and `macos-15` runners record their observed
   `runner.ImageOS`/`ImageVersion` and byte-compare outputs only when both
   runs landed on the same image; on mismatch the verification aborts with
   a re-run instruction (no claim is made that a workflow can pin the
   underlying image SHA; the check is post-hoc identity of environments).
2. *"Gradle exposes CI-safe entry points."*
   → **1a / 1b / 1c** each deliver their task group; **1d** wires the
   `verify-all.sh` byte-compare into CI and asserts the entry points run
   there without interactive input.
3. *"Release artifacts contain only intended Android ABIs, local extension
   code, and declared native-host packages."*
   → **1a** (extension artifact file-set inline expected-list assertion in
   `test/bundle.test.ts`, plus `validateExtensionManifest`), **1b** (APK ABI
   inspection via `unzip -l` or `apkanalyzer` in 1d's verify image), **1c**
   (bridge package file-set snapshot).
4. *"Platform signing can consume CI-provided credentials without embedding
   secrets."*
   → **OTP-25.** OTP-01 produces unsigned artifacts only; signing,
   notarization, and the CI-credential plumbing that consumes them are
   OTP-25's scope. OTP-01 contributes the unsigned-artifact shape and the
   no-embedded-secrets guarantee (nothing in 1a/1b/1c ever writes a
   credential to disk or to logs). OTP-01 is not sufficient evidence for
   this clause and does not attempt to be.
5. *"Signed outputs are verified for provenance and content correspondence
   but are not required to be byte-identical."*
   → **OTP-25.** The signed↔unsigned comparison procedure is defined and
   exercised once real signing exists. OTP-01 prepares the ground by
   producing stable unsigned reference artifacts whose bytes can later be
   compared against a signature-stripped signed build.
6. *"No runtime download or remote executable code is introduced."*
   → **1a / 1b / 1c** — enforced by: exact-pinned `package.json` with
   committed lockfile (no caret ranges), no `curl \| sh` or `npx` in any
   Gradle task, Tauri's `windows = []` preventing updater wiring, the
   manifest/CSP baseline (no remote script sources), and a license/SBOM scan
   in 1d that fails the build if any dependency declares a network-fetch
   install-time command.

### Decomposition-specific criteria (added for 1a–1d sequencing, not in the RFC)

7. Byte-determinism of `web-extension/dist` → zip → sha256 sidecar is
   demonstrated on the authoring machine **and** inside the pinned Docker
   verify-image before 1d closes (the actual cross-environment byte-compare
   gate that clause 1 requires).
8. Root `./gradlew check` runs `extensionFormat`, `extensionLint`,
   `extensionTypecheck`, `extensionTest` (source-only) without building the
   extension; `extensionPackage` and `extensionArtifactTest` run on-demand.
9. Root `./gradlew clean` removes `web-extension/dist/` in addition to
   `build/` (per the explicit exception in Global decision 3).

## Error-handling rules (global)

- No Gradle task performs a network fetch other than `npm ci` /
  `cargo fetch` against a committed lockfile. Everything else is pinned.
- Clean failure fast: missing tool → exit code 2-equivalent message
  naming the missing tool and the pinning file that should have provided
  it.
- No auto-fix: lockfile drift, format drift, lint findings all abort
  rather than rewrite.

## Out of scope (explicitly, per RFC and this decomposition)

- SPAKE2 protocol implementation (OTP-03), protocol freeze (OTP-02),
  Android BLE service (OTP-06), Chrome native-messaging connector
  (OTP-07), installers with signing (OTP-25), any UI, any OTP integration,
  any runtime download.

## Risks and open questions

- Cargo and npm lockfile byte-determinism across hosts: different OS
  line-endings or registry mirrors can produce different lockfiles from
  the same input. Mitigation: lockfiles are committed and compared
  byte-for-byte in `verify/verify-all.sh`.
- Tauri headless-mode stability (`windows = []`) is a Gate D review item;
  1c's skeleton proves the build produces the right artifact shape, not
  that the runtime behaves.
- Vite plugin pinning: some Vite plugins float on minor versions
  unless explicitly pinned; the spec mandates exact-version pinning in
  `package.json` (no caret).
- WASM loading inside an MV3 CSP is a known sharp edge
  (`'wasm-unsafe-eval'` required in `content_security_policy`). 1a's
  manifest guard hard-fails on `wasm-unsafe-eval`; it becomes permitted
  starting in 1b when the WASM plumbing lands, and 1b's plan must update
  `validateExtensionManifest` to whitelist exactly that one directive and
  no others.

## Documentation updates required

- `CLAUDE.md` "Build & Test Commands" section gains rows for
  `extensionPackage`, `rustJni`, `rustWasm`, `bridgePackage`,
  `verify/verify-all.sh`.
- `README.md` gains a "Sub-projects" section pointing at `web-extension/`,
  `rust/`, `native-bridge/`, `verify/`.
- `docs/reproducible-builds.md` gains a paragraph tying the new
  toolchains into the existing pinned-toolchain table.
